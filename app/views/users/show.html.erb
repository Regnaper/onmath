<% provide(:title, @user.name) %>
<% if @user.microposts.any? %>
  <h3>Сообщения (<%= @user.microposts.count %>)</h3>
  <ol class="microposts">
    <%= render @microposts %>
  </ol>
  <%= will_paginate @microposts %>
<% end %>